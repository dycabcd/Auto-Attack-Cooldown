# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx2G -Dorg.gradle.daemon=true
org.gradle.parallel=true
# ★ 本地 JDK 路径根据你的环境修改
# Windows: 填你的 JDK 17 路径
# Linux/macOS: 注释掉本行即可（Gradle 会自动从 PATH 找到 java）
org.gradle.java.home=D:\\ProgramData\\PyCharm\\PyCharm 2024.1.7\\jbr

# Forge Properties
minecraft_version=1.20.1
forge_version=1.20.1-47.3.7
mixin_version=0.8.5

# Mod Properties
mod_version=1.0.0
maven_group=dev.gamebuddy
archives_base_name=AutoAttackCooldown
